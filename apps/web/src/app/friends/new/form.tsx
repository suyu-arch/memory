'use client';
import { useState } from 'react';
export function NewFriendForm(){const [message,setMessage]=useState('');async function submit(data:FormData){const response=await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL ?? 'http://localhost:4000/v1'}/people`,{method:'POST',headers:{'content-type':'application/json','x-user-id':'demo-user','x-user-email':'demo@example.test','x-user-name':'小满'},body:JSON.stringify({displayName:data.get('displayName'),nickname:data.get('nickname')||undefined})});if(response.ok){location.href='/friends'}else setMessage(await response.text())}return <form className="form" action={submit}><div className="field"><label>名字</label><input name="displayName" required placeholder="朋友的名字"/></div><div className="field"><label>你习惯的称呼</label><input name="nickname" placeholder="例如：小林"/></div>{message&&<p>{message}</p>}<button className="button orange">添加朋友</button></form>}
